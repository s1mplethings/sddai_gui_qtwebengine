# Graph Spider - Spec（可自检示例）

## AC（验收标准）
1. 首屏必须能看到结构节点（不是空白）
2. 节点默认显示名字（LOD 可做，但重要节点必须可见）
3. 连线存在（可见节点之间应渲染边）
4. Hover：节点变大 + 相连边高亮
5. 单击不应直接跳走；至少有 Back/Up/Overview 返回方式

## CHECKS
见：`specs/graph_spider.checks.json`
